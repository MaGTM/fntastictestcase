import React from 'react';
import s from './ChannelChat.module.scss';

const ChannelChat = () => {
    return (
        <div className={s.ChannelChat}>
            CHAT
        </div>
    );
};

export default ChannelChat;
