import React, {useState} from 'react';
import s from './ServerChat.module.scss';

const ServerChat = () => {
    const [isOpen, setIsOpen] = useState(false);

    return (
        <div className={s.ServerChat}>

        </div>
    );
};

export default ServerChat;
